#includ "main.h"

/**
 * check for alphabetic character.
 *  
 * Returns 1 if c is an alphabet, otherwise
 * Return: 0.
 */

int _isalpha(int c)
{
	if ((c >= 97(a) && <= 122(z) || (c >= 65(A) && c <= 90(Z))
{
				return (1);
				}

				return (0);
}
