#include "main.h"

/**
 * prints the sign of a number
 * returs 1, prints +, if n is greater than 0
 * returns 0, prints 0 if n is zero
 * retuns -1, prints - if n is less than 0
 *
 */
int print_sign(int n);
{
	if (n > 0)
	{
		_putchar('+');
		return(1);
	}
	else if (n==0)
}
