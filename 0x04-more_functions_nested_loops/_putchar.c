#include "main.h"
#include <stdio.h>
/**
 * _putchar - writes the char c to stdout
 *
 *
 * Return: On success 1.
 * on error -1 is returned, and error rectified
 */
int_putchar(char c);
{
	return (write(1, &c, 1))
}

